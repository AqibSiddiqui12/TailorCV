# test_docx_generator.py
# Purpose: tests for docx_generator.py output structure.

# def test_generate_docx_returns_valid_bytes():
#     - Builds a sample TailorResponse fixture
#     - Calls generate_docx(), asserts output is non-empty bytes and opens
#       correctly via python-docx.Document(BytesIO(result))

# def test_section_order_respected():
#     - Fixture with out-of-order order_index values
#     - Asserts rendered doc's section headings appear in order_index order

# def test_cv_type_styling_applied():
#     - Parametrized over CVType.TECH / BUSINESS
#     - Asserts correct font name applied to generated document styles
