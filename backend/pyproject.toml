# pyproject.toml
# Purpose: single source of truth for backend deps (uv-managed, no requirements.txt).

# [project]
#   - name = "tailrcv-backend"
#   - dependencies: fastapi, uvicorn, pydantic>=2, pydantic-settings, anthropic,
#     pdfplumber, python-docx, redis, structlog, httpx (for tests)
#   - dev-dependencies: pytest, pytest-asyncio, pytest-cov, ruff, mypy

# [build-system]
#   - requires = ["hatchling"] (or setuptools) + build-backend

# TODO: run `uv init` / `uv add <pkg>` to populate this file for real,
# then commit the generated uv.lock alongside it.
