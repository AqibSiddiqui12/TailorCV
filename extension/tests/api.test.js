// api.test.js
// Purpose: Jest tests for api.js.

// test('postTailorRequest resolves with arrayBuffer on 200')
//   - Mocks global.fetch to return ok:true with arrayBuffer()
//   - Asserts returned value matches mocked buffer

// test('postTailorRequest throws with server detail message on non-200')
//   - Mocks fetch to return ok:false with json() error detail
//   - Asserts thrown Error message matches detail field

// test('postExtractRequest resolves with parsed JSON on 200')
//   - Mocks fetch to return ok:true with json() { resume_text, extraction_confidence }
//   - Asserts returned object matches
